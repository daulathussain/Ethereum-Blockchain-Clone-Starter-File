{
  "name": "hardhat-customchain-example",
  "version": "1.0.0",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.19.0"
  },
  "scripts": {
    "deploy": "npx hardhat run scripts/deploy.js --network theblockchaincoders",
    "compile": "hardhat compile",
    "clear": "rm -rf node_modules package-lock.json artifacts cache"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.3.1"
  }
}
